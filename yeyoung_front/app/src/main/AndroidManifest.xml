<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.yeyoung_front">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/koala"
        android:label="@string/app_name"
        android:roundIcon="@drawable/koala"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loginActivity"></activity>
        <activity android:name=".searchPageCameraActivity">
            </activity>
        <activity android:name=".showInitialCameraActivity"></activity>
        <activity android:name=".mypageActivity">
        </activity>
        <activity android:name=".searchPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>